export const fonts = {
    primary: 'Inter, sans-serif', // Fonte principal mencionada
    // Adicione outras fontes se identificar no Figma
  };
  
  export const fontSizes = {
    // Baseado nos exemplos da "Fonte Inter"
    h6: '13px',      // 0.8125rem (assumindo ser o menor tamanho)
    bodySmall: '14px', // 0.875rem (assumindo ser o próximo tamanho)
    body: '16px',    // 1rem
    bodyLarge: '21px', // 1.3125rem
    h5: '27px',      // 1.6875rem (assumindo ser o maior tamanho)
    // Adicione outros tamanhos conforme necessário (h1, h2, h3, h4, caption, etc.)
  };
  
  export const fontWeights = {
    regular: 400, // Peso padrão
    medium: 500, // Pode ser usado para alguns textos de destaque
    bold: 700,   // Para títulos e 강조
    // Adicione outros pesos se identificar
  };
  
  export const lineHeights = {
    body: 1.5, // Valor comum para corpo de texto
    heading: 1.2, // Valor comum para títulos
    // Adicione outros line heights se necessário
  };